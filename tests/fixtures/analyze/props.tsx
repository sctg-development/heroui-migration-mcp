import { useDisclosure } from "@heroui/react";

export const Comp = () => {
    return (
        <div>
            <ModalContent classNames={{}} motionProps={{}} disableAnimation />
        </div>
    );
};
